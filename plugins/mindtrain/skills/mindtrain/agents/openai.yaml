interface:
  display_name: "MindTrain"
  short_description: "Build training domains from your private references"
  default_prompt: "Use $mindtrain to organize a training domain from my local references, preview it, and save it after I confirm."
