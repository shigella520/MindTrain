interface:
  display_name: "MindTrain"
  short_description: "Train and track progress through MindTrain MCP"
  default_prompt: "Use $mindtrain to start today's knowledge training session."
dependencies:
  tools:
    - type: "mcp"
      value: "mindtrain"
      description: "MindTrain Trainer MCP"
      transport: "streamable_http"
      url: "http://127.0.0.1:8787/mcp"
